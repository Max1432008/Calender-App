document.getElementById("Hallo").textContent = "Was geht ab";

function getDaysInMonth(jahr, monat) {
  const anzahlTage = new Date(jahr, monat, 0).getDate();
  const tage = [];

  // hier eine Schleife, die das Array füllt

  return tage;
}
